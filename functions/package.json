{
  "name": "vantage-finance-functions",
  "description": "Backend proxy for Vantage Finance's Gemini/Groq AI calls and Google Play subscription verification. Keeps the real API keys and Play service account server-side (never in the Android app).",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "googleapis": "^144.0.0"
  }
}
